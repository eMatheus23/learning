{ 
    // Preset que detecta qual ambiente estaremos trabalhando. 
    "presets": ["@babel/preset-env"], 
    "plugins": [
        "@babel/plugin-proposal-object-rest-spread",
        "@babel/plugin-transform-async-to-generator"
    ]
}